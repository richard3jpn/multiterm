# ブラウザベース・マルチターミナルアプリ 要件定義書

## 1. プロダクト概要

- **プロダクト名**: MultiTerm（マルチターム）
- **概要**: ブラウザ上で動作し、任意の数のターミナルを動的に立ち上げ、レイアウトを自由に構成できるマルチターミナルアプリケーション。WindowsやUbuntuなど異なるOS環境に対応しつつ、Web技術を活かした高いカスタマイズ性とリッチなUIを提供する。
- **主要コンセプト**: 「AIエージェントとの協働に最適化された、状態がひと目でわかるワークスペース」

## 2. 機能要件

| カテゴリ | 要件 | 詳細 |
|---------|------|------|
| セッション管理 | ターミナルの任意数起動 | ユーザーが指定した数、動的にターミナルセッションを新規作成できる |
| セッション管理 | マルチOS・マルチシェル対応 | OS自動判定と適切なシェルの起動<br>・Windows環境：powershell.exe<br>・Ubuntu / macOS環境：bash または zsh |
| セッション管理 | プロセスの永続化 | バックエンドでプロセスを維持し、リロードしても状態が復元される |
| レイアウト管理 | 動的な画面分割 | 縦横分割による動的なレイアウト変更・パネル追加削除ができる（グリッド配置は将来拡張。6章参照） |
| レイアウト管理 | パネルサイズの調整 | 境界線をドラッグし、表示比率を自由に変更できる |
| UI・状態可視化 | エージェント状態の監視とUI連動 | Claude Code等の標準出力（stdout）を監視し、出力の静止とプロンプトパターン（7章の判定条件表で定義）から状態（Running / Idle / Waiting Input）を判定する |
| UI・状態可視化 | 状態に応じた動的スタイリング | 判定した状態に応じて、ターミナル枠・ヘッダー帯・状態ドット・画面全体の外枠の色をリアルタイムに変更する（配色は11.3。赤・オレンジ・青の3系統） |
| UI・状態可視化 | テーマ・ビジュアル設定 | プリセットテーマ（配色）を一括で切り替えられる（背景画像・透明度カスタマイズはスコープ外。6章参照） |
| UI・状態可視化 | フォント設定（v2） | ターミナルのフォントファミリー（等幅プリセット）とフォントサイズを設定でき、全ターミナルへ即時反映される（9章参照） |
| セッション管理 | シェル選択（v2） | 新規ターミナル作成時に、バックエンドが検出した利用可能シェルの中から起動シェルを選択できる（9章参照） |
| セッション管理 | セッション名変更（v2） | 開いているターミナルに任意の名前をつけられる（9章参照） |
| 入出力・通信 | PTY（擬似端末）通信 | WebSocketを経由し、バックエンドのPTYと入出力を同期する |

## 3. 非機能要件

- **クロスプラットフォーム対応**: バックエンドはWindowsとUbuntuの双方で同一コードで動作すること。
- **状態検知の軽量化**: 標準出力の監視処理が、大量のログ出力時にパフォーマンスのボトルネックにならないよう、正規表現による検知を最適化すること。
- **パフォーマンス**: アニメーションや視覚効果を多用しても、大量のログ出力時にブラウザが描画遅延を起こさないよう、xterm.js のWebGLアドオンを活用すること。

## 4. システムアーキテクチャ・技術スタック

> **v4（2026-08-31改訂）**: 軽量化のため Node.js から Rust へ移行し、Docker/Terraform 構成を廃止した。
> 移行の経緯・実測値は BUILDLOG.md（Phase 18以降）を参照。旧構成は 4.1 に残す。

- **フロントエンド**: Preact, TypeScript
- **UI・スタイリング**: Tailwind CSS + 自前コンポーネント（状態に応じた動的なクラス切り替えでエフェクトを制御）
- **ターミナル描画**: xterm.js（@xterm/addon-webgl 併用）
- **状態監視ロジック**: バックエンド（Rust）側でPTYの出力ストリームを監視して状態を判定する（判定条件は7章）
- **バックエンド**: Rust + axum（HTTP・WebSocket・画面の静的配信をすべて担う単一プロセス）
- **PTY管理**: portable-pty（Windows ConPTY / Unix openpty を同一APIで扱う）
- **通信プロトコル**: WebSocket（バイナリフレーム。詳細は10章）
- **配布形態**: rust-embed でフロントのビルド成果物を実行ファイルへ埋め込む。実行時に Node.js は不要

### 4.1 旧構成（v3まで。参考）

React / shadcn/ui / Node.js + Express / node-pty / Terraform + Docker provider による2コンテナ構成。
Docker・Terraform は v4 で廃止し、Windows ホストでの直接起動のみをサポートする。

## 5. 実装範囲（MVP）

生成先ディレクトリ: プロジェクト直下（`frontend/`・`backend-rs/`）。

1. **セッション管理**: ターミナルセッションの任意数起動・破棄（REST API + WebSocket）
2. **マルチOS・マルチシェル対応**: 実行環境の自動判定（win32: powershell.exe / それ以外: `$SHELL` または bash）
3. **PTY通信**: WebSocket経由でPTYの入出力をフロントエンドと同期（リサイズ含む）
4. **プロセスの永続化**: バックエンドプロセスが生きている間はPTYを維持し、ブラウザリロード後に既存セッションへ再接続・画面バッファ復元できる
5. **動的レイアウト**: パネルの縦横分割・削除、境界線ドラッグによるサイズ調整
6. **状態判定と可視化**: stdout監視によるRunning / Idle / Waiting Input判定と、状態別スタイル（配色は11.3）
7. **テーマ切替**: プリセットテーマ（dark / light の2種）の一括切替
8. **描画性能**: xterm.js WebGLアドオン有効化（非対応環境はデフォルトレンダラにフォールバック）
9. **フォント設定（v2）**: 等幅フォントプリセットからのファミリー選択とサイズ変更（10〜20px）。localStorageに保存し全ターミナルへ即時反映（詳細は9章）
10. **シェル選択（v2）**: 新規ターミナル作成時の起動シェル選択。バックエンド検出の許可リスト方式（詳細は9章）
11. **セッション名変更（v2）**: 開いているターミナルへの任意の名前付け（詳細は9章）
12. **アクセス制御（必須）**: 本アプリはPTYへの任意コマンド実行を提供するため、以下を必須要件とする
   - WebSocketハンドシェイク時に `Origin` ヘッダをホワイトリスト（フロントエンドのオリジンのみ）で検証し、不一致は接続拒否する
   - REST APIのCORSは同ホワイトリストのオリジンのみ許可する
   - ホストへの公開はループバック（127.0.0.1）限定とする（公開方法の詳細は8章）
13. **単一プロセス・単一ポート配信（v4）**: バックエンドがフロントのビルド成果物を実行ファイルへ埋め込み、画面・REST API・WebSocket を同一ポートから配信する。未知のパスは `index.html` を返す（SPAフォールバック）
14. **WebSocketバイナリプロトコル（v4）**: PTY入出力はタグ1バイト + ペイロードのバイナリフレームで送る。JSONエスケープを介さず生バイトのまま流す（詳細は10章）
15. **ターミナル一覧サイドバー（v4）**: 開いているターミナルとその状態を一覧表示する（詳細は11章）

## 6. 除外機能（スコープ外）

- 認証・マルチユーザー対応（ローカル個人利用前提。アクセス制御は5章12項のOrigin検証＋ループバック限定公開で行う）
- 背景画像・透明度などの詳細ビジュアルカスタマイズ（テーマはdark / lightプリセット切替、フォントは9章のプリセット選択のみ）
- グリッド配置レイアウト（MVPは二分木の縦横分割のみ。将来拡張）
- バックエンドプロセス再起動後のセッション復元（ディスク永続化）
- セッション検索などの管理機能
  - **ウィンドウ切り替えは v5 で導入した（14章）**。ターミナルはいずれか1つのウィンドウに属し、
    サイドバーで切り替える。それ以上の階層（ワークスペースの入れ子・タブのタブ）は導入しない
- クラウドデプロイ
- **Docker / Terraform によるコンテナ構築（v4で廃止）**。Windowsホストでの直接起動のみをサポートする

## 7. セッション・状態モデル

- **Session**: `{ id: string, title: string, shell: string, createdAt: string, status: SessionStatus }`
  - バックエンドがメモリ上で保持（`Map<id, PtySession>`）。DBは使用しない
  - タイトルは作成時に「Terminal N」で自動採番し、v2で名前変更API（9章）により変更可能
  - シェルは作成時に許可リスト（9章）から選択可能。省略時はサーバ既定シェル
  - 同時セッション数の上限は16。超過時はREST APIがエラーを返す
  - 各セッションは直近の出力バッファ（リングバッファ、上限200KB/セッション）を保持し、再接続時に再生する
- **SessionStatus**: `running` / `idle` / `waiting-input`
  - 判定はバックエンドの出力ストリーム監視で行い、変化時にWebSocketでフロントエンドへ通知する

### 状態判定条件表（対象: bash / zsh / fish / sh / powershell / cmd.exe / 汎用対話CLI（Claude Code等はベストエフォート））

| 状態 | 判定条件 |
|------|---------|
| running | 出力ストリームが継続中（静止時間 < 300ms）、または静止しているが末尾行がいずれのプロンプトパターンにも一致しない |
| idle | 出力が300ms以上静止し、かつ末尾行がシェルプロンプトパターンに一致: bash/zsh `/[$%#]\s*$/`、powershell `/^PS .*>\s*$/`、汎用（cmd.exe `C:\...>` を含む）`/>\s*$/` |
| waiting-input | 出力が300ms以上静止し、かつ末尾行が対話プロンプトパターンに一致: `/[?？]\s*$/`、`/\(y\/n\)/i`、`/\[y\/n\]/i`、`/password.*:\s*$/i`、`/続行しますか/` |

- 優先順位: waiting-input > idle > running（waiting-inputパターンとidleパターンが両方一致する場合はwaiting-input）
- **受け入れ基準**: 上記条件表を実装した判定器のユニットテストで、代表シナリオ（①長時間コマンド実行中→running ②コマンド完了しプロンプト表示→idle ③`read`・確認プロンプト・パスワード入力→waiting-input ④waiting-inputパターンとidleパターンが同時一致する末尾行→waiting-input（優先順位検証））の判定が一致すること。ヒューリスティックであるため実運用の誤検知はベストエフォートとして許容するが、条件表と実装の乖離は不許容
- **レイアウト**: 二分木モデル（split: horizontal | vertical、leaf: sessionId）。フロントエンドで保持し、localStorageへ保存する
  - **セッション一覧のSSOT（信頼源）はバックエンド**とする。ページロード・再接続時はバックエンドの生存セッション一覧を取得し、localStorageのレイアウト中に存在しないsessionIdを参照する葉ノードは自動削除（パネルを閉じた扱い）してからレンダリングする
- **ウィンドウ（v5）**: レイアウトを持つ面。`{ id, title, layout, activeSessionId }` の配列と、アクティブなウィンドウのIDを localStorage キー `multiterm.layout.v2` へ保存する（詳細は14章）
  - **セッションはいずれか1つのウィンドウにのみ属する（排他所属）**。PTYのサイズはセッションごとに1つしか持てないため、同じセッションを別々の大きさのペインへ同時に出すことはできない
  - バックエンドはウィンドウを知らない。セッションはフラットに管理され、上限16は全ウィンドウの合計に対して効く

## 8. 画面構成・セキュリティ・運用

- **画面**: 単一ページ（ワークスペース）のみ。ページ遷移はなく、複数のウィンドウを同一ページ内で切り替える（14章）
  - ヘッダー: サイドバー開閉、新規ターミナル作成、フォント設定、テーマ切替、全体状態の集約バッジ（11章。全ウィンドウ横断）
  - 左サイドバー: 開いているターミナルの一覧と状態。ウィンドウごとにグループ化する（11章・14章）
  - ワークスペース: アクティブなウィンドウの分割されたターミナルパネル群（各パネルに状態色の枠・タイトル・分割/閉じるボタン）。非アクティブなウィンドウは `display:none` でDOMに残す（14章）
- **認証**: なし（除外機能参照。アクセス制御は5章12項が必須要件）

### ネットワーク公開ポリシー（5章12項の実現方法）

| 実行形態 | バインド/公開 |
|---------|--------------|
| 通常起動（単一バイナリ） | `127.0.0.1` にバインドし、画面・API・WebSocketを同一ポートで配信する。許可オリジンはそのポート自身 |
| 開発モード（Vite併用） | バックエンドは `127.0.0.1` にバインドし、許可オリジンにVite開発サーバのオリジンを加える |

- いずれの形態でも、WebSocketのOrigin検証・CORS制限（5章12項）は有効であること（ネットワーク限定とOrigin検証の二重防御）
- **許可オリジンの設定源**: バックエンドは環境変数 `ALLOWED_ORIGINS`（カンマ区切り）でホワイトリストを受け取る
  - 通常起動: 起動スクリプトが `http://127.0.0.1:<port>` と `http://localhost:<port>` を注入する
  - 開発モード: 上記に加えてVite開発サーバのオリジン（既定 5174）を加える
- **フロントエンドの接続先**: `VITE_WS_URL` / `VITE_API_URL` 未設定時は配信元と同じオリジンへ接続する。
  ポートを変えても、localhost / 127.0.0.1 のどちらで開いても追従すること

### 運用・検証

- **デプロイ**: ローカルのみ。Windowsホストで実行ファイルを直接起動する（Docker/Terraformはv4で廃止）
- **Windows動作の受け入れ確認**: Windowsホスト上で起動し、cmd / PowerShell / WSLディストロでのセッション作成・入出力・状態判定を手動確認する（CI対象外。確認結果はBUILDLOG.mdに記録する）
- **状態管理**: Preact Context API / **テスト**: cargo test（backend） / Vitest（frontend、カバレッジ80%目標）

## 9. v2拡張要件（2026-07-25追加）

### 9.1 フォント設定

- ターミナルのフォントファミリーとフォントサイズを設定UIから変更できる
- **ファミリー**: 等幅プリセットからの選択のみ（任意文字列入力は不可）。プリセット:
  1. System Mono（既定: `Menlo, Consolas, "Liberation Mono", monospace`）
  2. Consolas（`Consolas, Menlo, monospace`）
  3. Courier New（`"Courier New", Courier, monospace`）
- **サイズ**: 10〜20px の整数。範囲外はUIで入力不可とする
- 設定はlocalStorageへ保存し、開いている全ターミナルへ即時反映する（再作成不要。xtermのオプション更新+再フィット）

### 9.2 シェル選択（許可リスト方式・セキュリティ要件）

- **GET /api/shells**: バックエンドが起動環境で利用可能なシェルを検出し、許可リストとして返す
  - 検出対象: Linux/macOS = `bash` / `zsh` / `fish` / `sh`（実在するもののみ）+ サーバ既定シェル、win32 = `powershell.exe`
  - レスポンス: `{ id, label, path }` の配列（envelope形式）
- **POST /api/sessions**: ボディ `{ shell?: string }` を受け付ける。`shell` は許可リストの `id` のみ有効
  - **許可リスト外の値・任意パス指定は 400 を返す**（PTYで任意バイナリを起動させないためのセキュリティ要件）
  - 省略時はサーバ既定シェルで起動する
- **Sessionモデルの `shell` フィールドは許可リストの `id`（表示用）を格納する**。実際の起動パスはバックエンド内部で許可リストから解決する
- fish / sh 選択時の状態判定は7章の汎用パターン（`/>\s*$/` 等）によるベストエフォートとする（条件表への専用パターン追加はスコープ外）
- フロントエンドは新規ターミナル作成UIで許可リストから選択できる（選択はlocalStorageに保存し次回既定にする）

### 9.3 セッション名変更

- **PATCH /api/sessions/:id**: ボディ `{ title: string }` でセッション名を変更する
  - バリデーション: 1〜30文字、制御文字（U+0000〜U+001F, U+007F）禁止、前後空白はトリム。違反は 400
  - 存在しないIDは 404
- フロントエンドはパネルタイトルのインライン編集（クリック→入力→Enter確定/Escキャンセル）で変更できる
- 変更後のタイトルはセッション一覧（GET /api/sessions）に反映され、リロード後も維持される（バックエンドがSSOT）

### 9.5 Windowsマルチシェル対応（v3）

VSCodeの統合ターミナルのように、新規ターミナル作成時に cmd.exe / PowerShell / WSLの各ディストロ（zsh・bash、ユーザーのdotfile込み）を選んで開けるようにする。

#### 実行構成

- 既存のDocker（Linux）構成は維持しつつ、**Windowsホストでの backend 直接起動**を正式サポート構成として追加する
  - 前提: Windows側に Node.js（v22系）が導入済み。node-pty@1.1.0 は prebuilt バイナリのためネイティブビルド不要
  - Windowsホストで起動した backend からのみ、Windows系シェル（cmd/PowerShell）とWSLディストロが検出・起動できる（Linuxコンテナ内からは不可）

#### シェル検出のWindows対応（GET /api/shells の拡張）

`os.platform() === 'win32'` のとき、以下を検出して許可リストに含める:

既存の `ShellInfo` は `{ id, label, path }`（9.2章）。`path` は実行ファイルパスを表す。以下の表の「path」列がその値。

| id 例 | label 例 | path | args |
|-------|---------|------|------|
| `cmd` | コマンドプロンプト | `cmd.exe` | `[]` |
| `powershell` | Windows PowerShell | `powershell.exe` | `['-NoLogo']` |
| `pwsh` | PowerShell | `pwsh.exe` | `['-NoLogo']`（存在する場合のみ） |
| `wsl-<distro>` | `<distro> (zsh)` 等 | `wsl.exe` | `['-d', '<distro>', '--cd', '~', '--', '<login-shell>', '-l']` |

- WSLディストロは `wsl.exe -l -v` の出力（UTF-16LE・カレントディストロの `*` マーカ・空白整形を含む）から列挙する。`docker-desktop` / `docker-desktop-data` 等の名前を含むシステムディストロは除外する。パースが失敗した場合はWSLシェルを一切追加しない（cmd/powershellは維持）
- 各ディストロのログインシェルは `wsl.exe -d <distro> -- sh -lc 'echo $SHELL'` で取得し、取得できない/存在確認できない場合は zsh→bash→sh の優先で `wsl.exe -d <distro> -- which <shell>` により在否確認して決定する

#### ShellInfo モデルの拡張

- `ShellInfo` に `args?: string[]`（**オプショナル**）フィールドを追加する。既存のLinux/macOS検出エントリは `args` 未指定のままでよく、GET /api/shells の既存レスポンス契約（`{ id, label, path }`）を壊さない
- PTY起動時は `spawn(shell.path, shell.args ?? [], ...)` の形で args を適用する（既存の空配列ハードコードを置き換える）。この配線は `PtySpawn` 型・`SessionManager.create`・`server.ts` の spawn 呼び出しの3点を更新する必要がある
- **セキュリティ**: path・args はバックエンドが検出時に構築した固定値のみを使う。クライアントは許可リストの `id` を指定するだけで、path・args・任意パス・任意引数をクライアントから注入できないこと（許可リスト方式の維持。許可リスト外idは400）。node-ptyへは args を**配列**で渡し、シェル文字列補間を発生させない

#### 状態判定

- cmd.exeプロンプト（`C:\Users\foo>`）とPowerShellプロンプト（`PS C:\...>`）は、いずれも7章の**既存の汎用idleパターン `/>\s*$/`** で idle 判定される（新パターン追加は不要）
- ただし7章条件表の「対象」にcmd.exe / powershell を明記し、条件表と実装の乖離（7章の不許容規定）を避ける

#### 既知事象・セキュリティ維持

- node-pty の `conpty_console_list_agent` が `AttachConsole failed` を stderr に出す場合があるが、シェルの対話動作には影響しない既知事象として扱い、実装側で握りつぶす（プロセスを落とさない）
- Windowsホスト直接起動時も、既存のセキュリティ要件（許可リスト方式・許可リスト外400・WebSocket/REST の Origin 検証・127.0.0.1 限定バインド）を維持する

#### 受け入れ基準（v3）

1. win32環境で GET /api/shells が cmd / powershell /（pwsh存在時）/ wsl-<distro> を返す（実在するもののみ）
2. 各シェルの args がモデルどおり構築される（特にwslの `-d <distro> --cd ~ -- <shell> -l`）ことをユニットテストで検証
3. 許可リスト外id・任意path/argsのクライアント指定は400（既存のセキュリティ要件維持）
4. cmd.exeプロンプト（`C:\Users\foo>`）が既存の汎用idleパターンで idle 判定されることをユニットテストで検証
5. （手動）Windowsホストでbackend起動時、cmd / PowerShell / WSL Ubuntu(zsh, .zshrc読込) が実機で対話動作すること

### 9.4 共通事項・受け入れ基準

- **アクセス制御**: 新設エンドポイント（GET /api/shells・PATCH /api/sessions/:id・shell指定POST）にも5章12項のサーバ側Origin強制・CORS制限を適用する
- **受け入れ基準（ユニットテストで検証）**:
  1. GET /api/shells が実在シェルのみを返す（存在しない候補は含まれない）
  2. POST /api/sessions: 許可リスト内のid→201で該当シェル起動 / 許可リスト外の値・パス文字列（`/bin/evil`等）→400
  3. PATCH /api/sessions/:id: 正常名→200で一覧に反映 / 空文字・31文字以上・制御文字含み→400 / 不存在ID→404
  4. フォント設定: プリセット選択とサイズ変更がlocalStorageへ保存され、設定値がxtermオプションに反映される

## 10. v4拡張要件: WebSocketバイナリプロトコル（2026-08-31追加）

### 10.1 背景

Node版はPTY出力1チャンクごとに、サーバ側で `JSON.stringify`、クライアント側で `JSON.parse` を行っていた。
大量ログ出力時にこのエンコード／デコードがボトルネックになるため、生バイトのまま流す形へ変更する（RDD 3章の非機能要件「描画遅延を起こさない」への対応）。

### 10.2 フレーム形式

タグ1バイト + ペイロード。

**サーバ → クライアント**

| タグ | 種別 | ペイロード |
|---|---|---|
| `0x01` | data | PTY出力の生UTF-8バイト |
| `0x02` | replay | 再接続時のバッファ（生UTF-8バイト） |
| `0x03` | status | 1バイト（0=running / 1=idle / 2=waiting-input） |
| `0x04` | exit | i32 リトルエンディアン |
| `0x05` | error | UTF-8メッセージ |

**クライアント → サーバ**

| タグ | 種別 | ペイロード |
|---|---|---|
| `0x01` | input | 生UTF-8バイト（8KB上限。超過は破棄） |
| `0x02` | resize | u16 LE cols + u16 LE rows（0は破棄） |

- 不正なフレーム（未知のタグ・長さ不一致）は破棄する。外部データを信頼しない
- PTY出力は5ms窓でまとめて1フレームにして送る（WSフレーム数と xterm の write 呼び出しを削減）

### 10.3 バッファのバイト境界

出力バッファはバイト単位のリングバッファ（上限200KB/セッション、RDD 7章）とする。
バイト単位で切り詰めるとUTF-8の途中で切れるため、replay送信時に先頭の不完全なマルチバイト列を取り除くこと。

### 10.4 DSR（カーソル位置問い合わせ）の扱い

ConPTY はシェル起動時に `ESC [ 6 n` を送り、応答があるまでシェルの出力を開始しない。
**バックエンドがこの問い合わせに応答する**こと（`ESC [ 1;1R` を書き戻す）。あわせて、DSR自体はクライアントへ送らない。

- クライアント未接続でもシェルが起動する（Node版はxtermの応答が必要で、これができなかった）
- replayでxtermが二重に応答し、その応答が `;1R` としてプロンプトへ入力される問題も同時に防ぐ

### 10.5 受け入れ基準

1. 各フレームのパースがユニットテストで検証されている（正常系・長さ不一致・未知タグ・上限超過）
2. リングバッファがマルチバイト境界で切れた場合でも、replayが正しいUTF-8になる
3. （手動）WS未接続のままRESTでセッションを作成しても、シェルが起動して状態がidleへ遷移する
4. （手動）リロードを繰り返しても `;1R` 等がプロンプトへ混入しない

## 11. v4拡張要件: ターミナル一覧サイドバー（2026-08-31追加）

### 11.1 背景

分割が増えると、どのペインが入力待ちかを見落としやすい。
ペインを切り替えずに全体を見渡せる一覧を画面左に常設する。

### 11.2 表示内容

各行に、状態ドット・Alt番号（1〜9。対象外は非表示）・セッション名・シェルラベル・状態ラベルを表示する。

- 行をクリックするとそのターミナルがアクティブになり、キーボードフォーカスが移る
- 行から直接そのターミナルを閉じられる
- サイドバー上部に状態別の件数を集約表示する（該当0件の状態は表示しない。すべて待機なら「すべて待機」）
- ヘッダーに全体の集約状態をバッジ表示する（待機のみのときは非表示）
- サイドバーはヘッダーのボタンで開閉できる

### 11.3 ペイン状態（表示用の4段階）

バックエンドの `SessionStatus`（RDD 7章）は変更しない。表示側で「見たかどうか」の軸を足して4段階にする。

| 表示状態 | 条件 | 色 |
|---|---|---|
| blocked（入力待ち） | `waiting-input` | 赤 `red-500`（ドットのみ点滅） |
| working（実行中） | `running` | 青 `blue-500` |
| done（完了・未確認） | `idle` かつ未確認 | オレンジ `orange-300`（待機と同色。色では区別せず、集計の件数だけで示す） |
| idle（待機） | `idle` かつ確認済み | オレンジ `orange-300` |

**配色の意図**: 赤・オレンジ・青の3系統で組む。エージェントを見張る使い方では、実行中は放っておけて、
入力待ち（承認を求めている）と待機（作業が終わって手が空いた）こそ気づきたい。そのため目につくオレンジを
待機に、落ち着いた青を実行中に割り当てている。枠・発光・ドット・ヘッダー帯・集約バッジ・サイドバーの
集計をすべて同じ色で揃える。

**画面全体の外枠**: 集約状態をウィンドウの縁にも出す。内側に描く（inset）ため枠の有無でレイアウトがずれない。
入力待ちは赤3px＋内側発光、待機・完了はオレンジ3px、実行中は枠なし（放っておいてよいため）。

- **集約**は blocked > working > done > idle の優先順で、最も注意が必要な状態を全体の状態とする
- **「完了（未確認）」と記録する条件**: 実行中から待機へ変わり、かつそのターミナルがアクティブでなく、かつ実行が一定時間（1秒）以上続いた場合。
  再接続時のリサイズ等でPTYが再描画し `running` が一瞬立つケースを完了と数えないための下限
- **解除条件**: そのターミナルをアクティブにしたとき

### 11.4 スコープ外

セッション検索などの管理機能は導入しない。

v5でウィンドウの階層を1段だけ導入した（14章）。サイドバーはウィンドウ見出しでグループ化するが、**表示するのは全ウィンドウのターミナル**であり、アクティブなウィンドウに絞り込まない。絞り込むと他ウィンドウの入力待ちを見落とし、本サイドバーの目的（切り替えずに全体を見渡す）を損なうため。

### 11.5 受け入れ基準

1. 状態決定・集約・集計・完了判定が純関数としてユニットテストで検証されている
2. 「最初から待機」のターミナルが完了として表示されないこと
3. リロード後に全ターミナルが完了として表示されないこと
4. （手動）別ペインを見ている間に完了した処理が「完了（未確認）」として残り、その行をクリックすると「待機」へ戻ること

## 12. v4拡張要件: エージェント別の状態判定（2026-08-31追加）

### 12.1 背景

7章の判定条件表は汎用パターン（`?` 終端・`(y/n)` など）と「代替画面で静止したら入力待ち」で構成されている。
このため Claude Code のような全画面TUIでは、**作業を終えて入力を待っているだけの状態も「入力待ち」**として扱われ、
承認を求めている状態と区別がつかない。分割が多いと「本当に対応が必要なペイン」が埋もれる。

herdr は「フォアグラウンドプロセスを識別し、そのエージェント専用のパターンで画面下部を照合する」方式を採り、
blocked（入力・承認待ち）は**既知の承認・質問・許可UIに一致したときだけ**としている。この考え方を踏襲する。

### 12.2 エージェントの識別

代替画面（TUI）に入っているセッションに限り、画面テキストからエージェントを識別する。

| エージェント | 目印（いずれかに一致） |
|---|---|
| Claude Code | `Claude Code v<数字>` / `for shortcuts` / `auto mode on (shift+tab to cycle)` |

- 識別はセッションごとに一度だけ行い、以後は保持する（毎チャンクの走査はしない）
- 通常シェルは識別の対象外とする（画面に同じ文字列が出ても誤検出しない）

### 12.3 Claude Code の状態判定

識別できたセッションでは、7章の汎用パターンではなく以下で判定する。

| 状態 | 条件 |
|---|---|
| running | 出力ストリームが継続中（静止時間 < TUI閾値 1000ms） |
| waiting-input | 静止しており、画面に承認・選択を求めるパターンがある |
| idle | 静止しており、承認・選択のパターンが無い（作業を終えて入力を待っている） |

承認・選択のパターン（Claude Code のパーミッションUIより）:

- `Do you want to proceed?`
- `Do you want to make / create / allow / run ...`
- `and don't ask again`
- `tell Claude what to do differently`
- `requires approval`
- `waiting for your input`

### 12.4 末尾バッファ

承認UIは画面下部に複数行で描画され、罫線1行が200文字を超えることがある。
7章の末尾512バイトでは選択肢まで届かないため、**エージェントを識別したセッションでは末尾8KBを保持する**。
評価は静止時にしか走らないため、常時のコストは増えない（RDD 3章の軽量化要件を維持）。

### 12.5 識別できないTUI

vim / less など識別できないTUIは、従来どおり「代替画面で静止したら入力待ち」とする（挙動を変えない）。

### 12.6 受け入れ基準

1. Claude Code の画面テキストからエージェントを識別できること。通常シェルの出力では識別しないこと
2. 入力を待っているだけの画面が waiting-input にならないこと（idle になること）
3. 承認UIの各文言で waiting-input になること
4. 識別できないTUIの挙動が従来と変わらないこと
5. 罫線等の再描画が挟まっても、承認UIが末尾バッファに残ること

## 13. v4拡張要件: 起動時間とシェル検出の分離（2026-09-01追加）

### 13.1 背景

12章までの実装では、`GET /api/shells` の許可リストを**サーバ起動前に**すべて確定させていた。
WSLディストロの検出はコールドスタートで10秒以上かかる（12章の実測）ため、
その間ポートが開かず、起動スクリプトからは「起動しない」ように見えていた。

実測: WSLウォームで7秒、コールドではタイムアウト上限（20秒）まで待たされる。

### 13.2 要件

- **サーバは即座に listen する**こと。外部コマンドの実行結果を待たない
- 起動直後は**外部コマンド不要なシェルだけ**を許可リストとして公開する
  （Windows: cmd / powershell、Unix: 実在確認のみで済む bash / zsh / fish / sh）
- pwsh・WSLディストロの検出は**背後で進め**、完了したら許可リストを差し替える
- 検出中かどうかをクライアントへ伝え、クライアントは完了まで取り直せること

### 13.3 検出状況の通知

`GET /api/shells` のレスポンスヘッダで返す。envelope の形（`{ success, data, error }`）は変えない。

| ヘッダ | 値 | 意味 |
|---|---|---|
| `x-shell-detection` | `detecting` | 検出継続中。一覧は増える可能性がある |
| `x-shell-detection` | `complete` | 検出完了。一覧は確定 |

開発モード（別オリジンのVite）からも読めるよう、CORS の `Access-Control-Expose-Headers` に含める。

### 13.4 クライアントの挙動

- 起動時に一覧を取得して表示する（この時点では cmd / powershell だけのことがある）
- `detecting` の間は一定間隔で取り直し、`complete` になったら止める
- localStorage の既定シェルを許可リストと突き合わせて矯正する処理は、**検出完了後にのみ**行う
  （検出途中の不完全な一覧で矯正すると、WSLを既定にしていた設定が消えてしまうため）

### 13.5 受け入れ基準

1. 起動から listen までが1秒未満であること（WSLの状態に依存しないこと）
2. 起動直後の `GET /api/shells` が `x-shell-detection: detecting` を返し、cmd / powershell を含むこと
3. 検出完了後の `GET /api/shells` が `complete` を返し、WSLディストロを含むこと
4. クライアントが検出完了後の一覧を反映すること

---

## 14. v5拡張要件: ウィンドウ切り替え（2026-09-04追加）

### 14.1 背景

ターミナルが増えると1画面の分割では収まらない。ペインが小さくなりすぎて、どれが何か
分からなくなる。ターミナルを複数の「ウィンドウ」に分けて置き、切り替えられるようにする。

ただし**切り替えたウィンドウの状態が見えなくなってはならない**。11章のサイドバーは
「ペインを切り替えずに全体を見渡す」ために作ったものであり、ウィンドウを増やすほど
監視できなくなるのでは本末転倒になる。

### 14.2 データモデル

```
TermWindow  = { id, title, layout: LayoutNode | null, activeSessionId: string | null }
WindowsState = { windows: TermWindow[], activeWindowId: string }
```

不変条件:

1. **排他所属**: セッションはいずれか1つのウィンドウの `layout` にのみ現れる
2. `windows.length >= 1`（最後のウィンドウは閉じられない）
3. `activeWindowId` は必ず既存のウィンドウを指す
4. 各 `activeSessionId` は自ウィンドウの `layout` 内に存在するか `null`

**なぜ排他所属か**: PTYのサイズはセッションごとに1つしか持てない。同じセッションを
別々の大きさのペインへ同時に出すと、両方から異なるリサイズが飛んで後勝ちになり、
負けた側の表示が崩れる。両方を正しく描く方法がないため、複数配置を許さない。

保存データが重複を含む場合は、先に現れたウィンドウの葉を残し、後続から除去する。

### 14.3 永続化と移行

- キー: `multiterm.layout.v2`。値は `WindowsState`
- **v2のキーが存在する場合は、たとえ壊れていても v1 へ戻らない**（レイアウトなしとして扱う）。
  戻すと、ユーザーが閉じたはずのウィンドウが古いレイアウトから復活してしまう
- v2が無く v1（`multiterm.layout.v1`）がある場合は「Window 1」1つに包んで移行する。
  **v2の保存に成功したときだけ v1 を削除する**（保存できない環境でデータを失わないため）
- 生存セッションとの突き合わせは7章のSSOT方針をウィンドウ単位に広げる。
  どのウィンドウにも載っていない生存セッションは**アクティブウィンドウ**へ足す
- 中身が空になったウィンドウも残す（ユーザーが作った面を勝手に消さない）

### 14.4 非アクティブウィンドウはDOMに残す

`display:none`（Tailwind の `hidden`）で隠し、`TerminalPanel` はマウントしたままにする。

**外せない理由**: アンマウントするとWebSocketが切れ、切り替えのたびに再接続・replay・
再描画が走る。さらに、サイドバーの状態表示は各パネルのWebSocket経由でしか届かないため、
非アクティブウィンドウのターミナルの状態が更新されなくなり、14.1の要件を満たせない。

**`display:none` を選ぶ理由**: xterm は screen 要素に `IntersectionObserver` を張っており、
`display:none` になると描画を自動的に一時停止する（WebGLのdraw callも止まる）。
`visibility:hidden` や `opacity:0` ではこの検知が働かず、全ウィンドウの端末が描画され続ける。

**必要なガード**: 隠れている間は要素サイズが0になり、`FitAddon` が正しく測れない。

1. `ws.onopen`: 非表示のままWSが開くと `cols/rows` が既定値（80×24）のまま。
   それをPTYへ送ると実行中のTUIが80桁に潰れる。**表示中のみ送る**
2. `ResizeObserver`: `display:none` で 0×0 発火する。**表示中のみフィットする**
3. 表示に戻ったとき: `requestAnimationFrame` を挟んでから測り直して送る
   （同フレームでは親のサイズが取れない）。`proposeDimensions()` が NaN の間は送らない

**既知の制約**: Chrome は1ページあたりのWebGLコンテキストを約16個に制限する。
セッション上限16と一致するため、上限まで開くと古いコンテキストが失効しうる。
`webgl.onContextLoss` でDOMレンダラへフォールバックするため壊れはしない
（単一ウィンドウで16分割した場合も同じ）。

### 14.5 キーボード

| キー | 動作 |
|---|---|
| Alt+1〜9 | アクティブウィンドウ内の視覚順N番目のペインへ移動（9.6章から変更なし） |
| Alt+Shift+1〜9 | N番目のウィンドウへ切り替え |

**Ctrl+数字は使わない**。ブラウザがタブ切り替えに予約しており、ページ側の
`preventDefault()` では奪えないため。判定は `event.code`（`Digit1` 形式）で行い、
キーボード配列とShiftによる文字の変化に依存させない。

サイドバーのAlt+数字の序数は**アクティブウィンドウの行にだけ**表示する。
数字は「いま見ているウィンドウのN番目」を指すため、他ウィンドウの行に出すと嘘になる。

### 14.6 ウィンドウを閉じる

中のターミナル（シェルのプロセス）も終了する。排他所属のため、残すとどのウィンドウにも
属さないセッションができ、次回起動時にアクティブウィンドウへ流れ込んでしまう。

- **実行中（running）・入力待ち（waiting-input）が1つでも含まれる場合のみ確認する**。
  全部待機なら確認しない
- 削除は1つ失敗しても残りを続行し、失敗件数をエラー表示する
- 最後の1つは閉じられない

### 14.7 スコープ外

- ウィンドウ間のセッション移動（ドラッグ&ドロップ等）
- ウィンドウ構成のサーバ保存（5章12項のループバック限定という前提から、別PCで開く要件がない。
  セッションはバックエンド、レイアウトとウィンドウはブラウザ、という責務分離を保つ）
- ウィンドウの入れ子・タブのタブなど、これ以上の階層

### 14.8 受け入れ基準

1. ウィンドウの追加・削除・改名・切り替え・名前の自動採番・生存セッションとの突き合わせ・
   v1からの移行・キー判定・閉じる確認の判定が、純関数としてユニットテストで検証されている
2. 排他所属の不変条件が、重複を含む壊れた保存データに対しても保たれる
3. 非アクティブウィンドウのターミナルが、切り替え後もWebSocketを維持している
   （切断メッセージが出ない・出力が残っている）
4. 非表示のままWebSocketが開いても、PTYのサイズが既定値へ縮まない
5. 非アクティブウィンドウのターミナルの状態が、サイドバーの見出しと上部の集計に反映される
6. v1のデータしかない状態から起動すると、Window 1 に包まれて移行し、v1が削除される
7. ウィンドウを閉じると、中のセッションがバックエンドからも消える
